<Window x:Class="Celic.RepProWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Celic"
        mc:Ignorable="d"
        Height="150"
        Width="354"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>
    <StackPanel HorizontalAlignment="Left"
                VerticalAlignment="Top">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Генерация отчета завершена на "/>
            <TextBlock Text="{Binding StatusReport}"
                       Width="30"/>
            <TextBlock Text="%"/>
        </StackPanel>
        <ProgressBar Margin="0,10"
                     HorizontalAlignment="Stretch"
                     Height="20"
                     Width="350"
                     Minimum="0"
                     Maximum="100"
                     Value="{Binding StatusReport}"/>
        <TextBlock Text="Этап генерации отчета:"/>
        <TextBlock Text="{Binding ResultReport}"/>
    </StackPanel>
</Window>
